import React from 'react';
import { ContextMenuWrapper } from './ui.types';
export declare const DEFAULT_CONTEXT_ITEMS: string[];
export default function ContextMenu({ editorState, parent, toShow }: ContextMenuWrapper): React.ReactPortal;
